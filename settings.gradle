if (JavaVersion.current() < JavaVersion.VERSION_21) {
    throw new GradleException("This build must be run at least with java 21")
}

rootProject.name = 'yoframework'
include 'core', 'db', 'extra', 'quartz', 'web', 'morphia', 'ebean', 'grpc', 'hystrix', 'ssh', 'jdbi'
