before_install:
  - ./ensure-java-26 install
  - if ! ./ensure-java-26 use; then source ~/.sdkman/bin/sdkman-init.sh; fi
install:
  - java -version
  - ./gradlew clean compile test detekt localPublish
