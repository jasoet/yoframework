before_install:
  - ./ensure-java-24 install
  - if ! ./ensure-java-24 use; then source ~/.sdkman/bin/sdkman-init.sh; fi
install:
  - java -version
  - ./gradlew clean compile test detekt localPublish
